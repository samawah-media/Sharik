# Persistent workflow contract

Team submission creates a version and internal-only assets/comments. Internal approval records the exact version. Client submission can expose only that version. Client approval or change request binds to the same version, pauses/resumes SLA as required, and appends audit evidence. Delivery requires the final authorized state and closes the deliverable/package ledger transactionally.
