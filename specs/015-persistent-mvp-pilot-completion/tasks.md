# Tasks: Persistent MVP Pilot Completion

## SIL-52 / SIL-54 correction queue — 2026-09-10

- [x] Owner authorizes the two UAT blocker fixes; capture dirty baseline and investigate independent root causes.
- [x] Specify read-only last-sent snapshot contract and ADR; prepare authenticated DB regression before policy implementation. SQL execution remains a separate unchecked gate.
- [x] Add failing local client-read/actionability regressions, implement scoped projection and review it; include mixed-role mapping regression.
- [ ] Add failing package-consumption regressions, correct shared projection and verify SQL capacity consistency.
- [ ] Execute real database RED/GREEN and independent security review before hosted migration.
- [ ] Run combined local verification, rebuild and validate reviewed Preview before fresh fictional full lifecycle/file checks.
- [ ] Update owner checklist with exact results; retain unexecuted viewer/mobile/isolation/recovery tests and OWNER_UAT_HOLD.

## SIL-44 client workspace selector — 2026-09-10

- [x] Owner approves bounded design; independent security review completed.
- [x] Add tested authorized-scope resolver and validated session-cookie action.
- [x] Add accessible client workspace selector and component tests (delegated).
- [x] Integrate shared selection across portal; preserve actual-resource detail authorization.
- [x] Run targeted regression, typecheck/lint and independent diff review. Combined 216 tests green; production build passed. Local browser with hosted UAT verified selection and reload persistence; see [checkpoint](evidence/client-workspace-checkpoint.md).
- [ ] Publish reviewed Preview and verify Madar/Jidei selection live; resume pending client lifecycle UAT.

## UI implementation queue — 2026-09-08

- [ ] CI recovery: review and commit the approved source without secrets/client
  workbooks, hold automatic deployment of this branch, run exact-source clean
  database and persistent gates on GitHub, then verify UAT compatibility before
  Preview publication. Owner approved the test-location/order exception only.

- [ ] Publish approved owner Preview after compatibility gates. Preflight BLOCKED:
  pending invitation migration, unavailable local DB runtime and rejected saved
  admin test credentials. [Evidence](evidence/ui-owner-preview-preflight.md).
- [x] Owner approved visual direction and three-screen mockups; start requested.
- [x] X010-B-7C-21 / UI1: calm light management/client shell; local regression checks
  and independent review. See [UI1 execution plan](evidence/ui1-plan.md).
  Browser GREEN: 28 PASS / 20 intentional profile skips, session3861; owner/hosted
  acceptance remains open. [Recovery evidence](evidence/ui2-recovery-checkpoint.md).
- [x] UI2: dashboard hierarchy and source-backed chart, scoped local verification;
  owner requested execution
  before restart, browser QA deferred. [Scoped plan](evidence/ui2-plan.md).
  - [x] Dashboard and complete-read boundary implemented after observed RED.
  - [x] Independent scoped source review accepted both production files.
  - [x] Focused component GREEN: 25/25 tests (2 files), session 85518.
  - [x] Scoped local automated and integrated visual acceptance: component286,
    dashboard E2E6, TypeScript/lint exit0; see [checkpoint](evidence/ui2-recovery-checkpoint.md).
    Owner/hosted and long-name browser coverage remain outside this local result.
- [x] UI3: internal drawer presentation and concise copy, LOCAL PASS; [scoped plan](evidence/ui3-plan.md).
  - [x] Bounded layout/copy implementation and component regressions.
  - [x] Independent source review; browser12/12, component303/303,
    TypeScript/scoped lint exit0;375/1440 screenshots inspected.
    [Evidence and unrun gates](evidence/ui3-checkpoint.md). Not owner/hosted acceptance.
- [x] UI4: client approval presentation and concise copy, LOCAL PASS; [scoped plan](evidence/ui4-plan.md).
  - [x] Bounded presentation implementation and component RED/GREEN.
  - [x] Independent review; browser51PASS/9profile skips, isolated visual6PASS,
    full components317PASS, TypeScript/scoped lint exit0.
    [Evidence and unrun gates](evidence/ui4-checkpoint.md).
- [x] Consolidate UI1–UI4 scenarios in existing owner walkthrough as not run.
- [ ] Execute owner/hosted gates only with their prerequisites satisfied.

No owner UAT checkbox is passed by design approval. Earlier research-only
approval boundaries below are historical, superseded for the scoped UI1/UI2 slices.

## Approved research-only continuation — 2026-09-08

- [x] X010-B-7C-20: three Saudi reference platforms, proposed voice guide,
  three standalone role-based mockups and bounded design handoff; no app changes.
- [x] Owner approval of the proposed visual direction and three screens (2026-09-08).
- [ ] After design approval: scope implementation slices in spec/plan/tasks and
  consolidate executable scenarios in the existing owner walkthrough.

See [research package](evidence/ui-research-20260908/README.md).
The 20 owner requirements-quality items remain unchecked. Research approval
does not authorize production implementation or establish OWNER_UAT_PASS.
The following September 7 state is historical and remains valid for D17/D18.

## Active approved density batch — 2026-09-07

- [x] X010-B-7C-18 / D17: locally verified client decision-panel layout. Test button
  height at desktop/mobile, visible required reason and action order; preserve
  exact version/revision/idempotency and viewer/incomplete restrictions.
- [x] X010-B-7C-19 / D18: locally verified compact collapsed drawer execution-task rows. Retain
  readable title/assignee/date/status, >=44px native disclosure, complete
  editor, permission variants, validation focus and retained failed values.

D17 / X010-B-7C-18 and D18 / X010-B-7C-19 are LOCAL PASS. Lead six-file
regression session 31896 exited 0: 59 PASS / 25 intentional profile SKIP (5.5m),
including all six D18 cases passing again after the 6 PASS diagnostic (4.4m).
Independent audit and viewed screenshots support acceptance. Lead accepts the
installed Turbopack plus /work warmup for the shared local fixture harness;
no dependency, guard or assertion changes and no proven root-cause claim.
Fresh node check, scoped ESLint and diff check exited 0 (lead-reported).
No local successor is approved. Exact-HEAD CI/target confirmation and owner
walkthrough remain pending; all 20 owner checkboxes remain unchecked.
See [diagnostic evidence](evidence/d18-report.md#current-shared-harness-checkpoint--2026-09-07).

### Historical D17/D18 checkpoint — before warmup diagnostic

Both layouts implemented after observed browser RED; independent spec/quality
reviews found no issues. Lead full component: 38 files / 263 tests PASS.
First shared browser: exit 1, 11 PASS / 5 FAIL / 5 intentional SKIP (6.9m).
All four D17 geometry cases, six pending-inbox regressions and D18 desktop1440
passed. Five other D18 cases failed opening the drawer before geometry.
Reviewed test-only bounded React readiness correction retains one click and
all geometry assertions.
Corrected six-case D18 rerun: exit 1, 4 PASS / 2 FAIL (9.4m).
Mobile and RTL profiles at 1440/375 passed. Desktop1440 passed geometry,
interactions and DOM stress, but failed the final console assertion:
Next Router action dispatched before initialization. Desktop375 failed the
30s React readiness poll before geometry; its cause remains unproven.
D18 remains implemented, verification-pending and unchecked. Standalone
six-file scoped ESLint and git diff check exited 0. Typecheck PASS, exit 0.
Final isolated build PASS, exit 0: compiled in 2.5min, TypeScript 20.5s,
11 static pages. D17 / X010-B-7C-18 is LOCAL PASS and checked; D18 /
X010-B-7C-19 remains unchecked, implemented and verification-pending.
Wave-owned verification processes have ended. Final normal git diff check
passed; source changes remain local and unstaged.
All 20 owner checkboxes remain unchecked (lead verified).
Next bounded diagnostic: distinguish Next dev cold-start/HMR effects from
product behavior. The completed build is not production-mode browser
verification. `src/server/navigation/route-fixture-env.ts` disables `as=` actor
fixtures under `NODE_ENV=production`; do not bypass that guard. Subsequent
production-mode browser verification requires an approved authenticated DB
setup and has not been executed. External gates remain pending.
No next fix, dependency upgrade or error filtering is implemented/approved.
Images: evidence/visual-20260907 (D17 desktop/mobile, D18 desktop).
All 20 owner checkboxes remain unchecked; real-DB/hosted/CI/deployment
acceptance is not established by this checkpoint.

## Milestone 0–1: consolidation

- [x] T001 Correct Stage 2C gate integrity and hydration regression.
- [x] T002 Commit corrected Stage 2C baseline locally.
- [x] T003 Create canonical Spec 015 package and execution spine.
- [x] T004 Run Spec Kit checklist/consistency analysis and record evidence.

## Milestone 2: persistent foundation

- [x] T005 Apply and review persistent MVP migration.
- [x] T006 Add and execute schema/RLS tests for tenant, same-tenant client, role-negative, composite-FK, append-only, atomicity, and idempotency behavior.
- [x] T007 Add persistent repository contracts for versions, approvals, comments, files, and SLA segments.
- [x] T008 Add audited server commands for internal review, internal approval, client submission, client decision, delivery, and closure.

## Milestone 3: workflow wiring

- [x] T009 Replace production route fixture reads with scoped persistent reads.
- [x] T010 Wire and DB-verify account_manager/content_writer/designer assigned-team submission and internal review UI.
- [x] T011 Wire client approval/change-request UI with exact-version and scope checks.
- [x] T012 Persist SLA pause/resume and package consumption in the same audited workflow.
- [x] T013 Add complete persistent role-based E2E journey and secrecy assertions.

## Milestone 4: local acceptance

- [x] T014 Run lint, typecheck, unit, integration, component, RLS simulator, RLS DB, E2E, secret scan, diff check, and build.
- [x] T015 Run manual RTL/mobile/keyboard matrix and record evidence.
- [x] T016 Burn down P0/P1 and disposition every P2.
- [x] T017 Record local acceptance only if DB-backed checks are green.

## Milestone 5: handoff preparation only

- [x] T018 Prepare bounded Hadna-only hosted UAT prompt, approvals, stop conditions, rollback owner, and T032 closure evidence requirements.
- [x] T019 Confirm hosted actions performed: zero; Production acceptance: not granted.

## Owner-Authorized Hosted Team UAT Amendment

Historical status below applies only to its previously reviewed revision.
It is superseded for the current dirty continuation by evidence/gate-status.md;
real-DB, exact-HEAD CI, hosted/Preview and all 20 owner checks remain pending.

Status: `HOSTED_TEAM_UAT_READY_FOR_OWNER_TRIAL`. The rejected Preview was corrected, placeholder-only client reviews and viewer write access were closed, and the local/CI/hosted automated gates are green. The owner human walkthrough is the only remaining X008-H acceptance action; Production acceptance remains not granted.

The following hosted tasks extend Spec 015 only. They do not reopen or invalidate completed local tasks T001-T019.

- [x] H001 Hosted target and branch preflight: confirm clean worktree, fetch origin, inspect merge base, commit list, full diff/stat, migrations, generated files, secrets, and unrelated historical work.
- [x] H002 Rollback and stop-condition approval: record deployment, database, access rollback, rollback owner, executor, execution window, stop authority, expected rollback time, and verification steps before hosted mutation.
- [x] H003 Draft PR and CI: run full local verification, push the safe reviewed branch, create a Draft PR, inspect CI, and fix only in-scope failures. Do not merge.
- [x] H004 Supabase UAT migration gate: the originally reviewed set and later additive Spec 015 migrations were inventoried, reviewed, applied to non-Production UAT, and revalidated before hosted lifecycle acceptance.
- [x] H005 Synthetic Hadna UAT seed: create minimal idempotent run-ID-scoped Hadna tenant/client/contract/package/deliverable/file metadata records and record category/count-only evidence.
- [x] H006 Team account/access setup: configure only approved individual Samawah team testers and team-controlled client personas; if approved email mapping is unavailable, stop with `TEAM_ACCESS_INPUT_REQUIRED`. Owner-approved synthetic UAT accounts now cover management, account manager, assigned writer, assigned designer, unassigned internal negative tester, client approver, and client viewer; Auth, membership, and role checks pass for all categories.
- [x] H007 Vercel Preview deployment: configure Preview/UAT env only, deploy the reviewed branch to Preview/UAT, verify Supabase UAT binding, sign-in, Arabic RTL shell, no fixture actor support, non-local `APP_ENV`, and no service-role key in client bundles/logs. The protected UAT alias targets the exact reviewed Preview and owner-distributed protected access passed hosted smoke.
- [ ] H008 Hosted team workflow UAT: current real-Auth route/role evidence is green 27/27 on the corrected Preview, and historical bounded lifecycle evidence remains valid; close only after owner human PASS.
- [ ] H009 Defect burn-down and T032 evidence: no P0/P1 remains in the corrective technical slice and every P2 is fixed or explicitly dispositioned; retain open until the owner walkthrough records no new blocking defect.
- [ ] H010 Hosted handoff and boundary record: the corrected handoff is ready; close only after explicit owner PASS for management, assigned team, client viewer, and client approver.

Hosted amendment correction: H001-H007 remain accepted technical/environment evidence. H008-H010 are reopened by the 2026-07-19 owner trial. Automated route success must never be used as a substitute for understandable, representative human product acceptance.

## Product Experience Rescue Amendment — same Spec 015

- [x] X001 Amend Spec 015 with role outcomes, UX/security acceptance, hosted gates, and explicit boundaries.
- [x] X002 Establish the Samawah design contract and shared RTL/accessibility tokens.
- [x] X003 Make client pending approval navigation real and add a discoverable client shell/profile/sign-out path.
- [x] X004 Remove raw assignee identifiers from normal management card UI.
- [x] X005 Add persistent regression coverage for client pending/home/summary consistency and raw-ID secrecy. CI run `29239615839` passed RLS 228/228 and fixture E2E 108 for migrations 202607130001/002; later X006 migrations and persistent browser rerun remain open.
- [x] X006 Complete universal role-aware deliverable detail, persistent content/file/comment execution, and team board/list UAT. Exact-HEAD PR #37 quality run `29263587871` attempt 2 passed for commit `65191fdaf9319bc3b85a2d49d8c951c9c21e93ae`, including Supabase start/reset, RLS, fixture E2E, persistent E2E, secret scan, and build.
  - [x] X006-A Harden exact-current-version RPC invocation and scoped member-profile reads/sync with direct RPC and RLS tests.
  - [x] X006-B Implement the shared keyboard-accessible role-filtered deliverable drawer and persistent execution read model.
  - [x] X006-C Implement Zod/React Hook Form content editing, version submission, internal review/change/approval, and exact-version client send.
  - [x] X006-D Implement Uppy + Supabase Storage upload, metadata, cleanup, previews, signed access, and Storage/RLS tests.
  - [x] X006-E Implement persistent Tiptap comments with explicit visibility and client payload/markup secrecy tests.
  - [x] X006-F Implement assigned-work List/Board views, filters, five-to-six macro lanes, and governed dnd-kit interaction.
  - [x] X006-G Complete exception-first management and scannable client portal experiences.
  - [x] X006-H Execute DESIGN.md RTL/mobile/keyboard/reduced-motion/state visual QA and close all local gates.
- [ ] X007 Execute generic run-ID-scoped Glass/Hadna import, hosted persona UAT, defect burn-down, rollback/no-op evidence, and H008-H010 handoff. Import mechanics and automated lifecycle passed historically; representative data presentation and human acceptance are reopened under X008.
  - Execution sequence (subordinate runbook, not a second task source): [X007 Step-by-Step Execution Runbook](evidence/x007-step-by-step-execution-runbook.md).

## Owner Human Trial Rescue — same Spec 015

- [ ] X008 Correct the owner-rejected Preview experience without creating a new Spec or parallel plan.
  - [x] X008-A Remove duplicate navigation/actions and every client-name-specific hardcode from shared surfaces.
  - [x] X008-B Enforce understandable role entry/route boundaries and add a discoverable sign-out/profile control for every role shell.
  - [x] X008-C Quarantine synthetic lifecycle records from normal human-trial views without deleting their historical evidence.
  - [x] X008-D Present imported Glass/Hadna content as professional, reusable content cards with thumbnail or honest branded fallback, platform/format, caption excerpt, owner, status, SLA, and next action.
  - [x] X008-E Prepare a bounded representative Glass workflow so assigned team work is discoverable and at least one exact current version is genuinely waiting for the client approver; do not manufacture approval history.
  - [x] X008-F Fix corrupted display names and validate the imported field mapping using category/count/field evidence only.
  - [x] X008-G Add negative route/role checks, content-quality regressions, pending-count/action consistency checks, and desktop/mobile/RTL visual acceptance.
  - [ ] X008-H Deploy only the corrected reviewed Preview, obtain explicit owner PASS for all three experiences, then close H008-H010, X007, and T032 if no P0/P1 remains.

X008-A through X008-G are implemented and verified locally and on the corrected protected Preview. Corrected workbook evidence shows 16 imported deliverables/versions (15 content-plan and 1 coordination), 7 imported tasks, zero approvals, zero files, and zero meaningful values in the source caption/body columns; the earlier count of nine treated placeholder dashes as content and is superseded. One existing 1,042-character source suggestion was promoted without rewriting through an audited real-Auth version command, eight tasks remain assigned across four representative deliverables, and exactly one current version is now waiting for owner review with a semantic payload. Two placeholder-only pending items were safely retired after proving they had no client activity. Hosted boundary/persona smoke passed 27/27 across desktop, mobile, and Arabic RTL, including per-card payload validation, viewer read-only behavior, approver actions, no internal leakage, no raw UUID, and no mojibake. X008-H and the parent remain open only because explicit owner human PASS has not been recorded; H008-H010, X007, and T032 remain open.

2026-07-20 final X008-H technical correction: independent review first repaired the role, Arabic-label, hosted-runner, and persistent-reset gaps, then a second evidence audit found that dash placeholders had been counted as real review text and that `client_viewer` could still create client comments/uploads. Application head `220ec0ce90888de6c14c67f67e2bb4ca2758eb08` adds semantic review validation, fail-closed per-card hosted assertions, UI viewer read-only behavior, and additive PostgreSQL enforcement for comments/uploads. Exact-head quality run `29781410549` passed lint, typecheck, unit 54 files/208 tests, integration 28 files/112 tests, RLS simulator 8 files/24 tests, pgTAP 6 files/427 tests, component 21 files/72 tests, fixture E2E 126, persistent E2E 4, secret scan, and build. The migration was applied only to non-Production UAT; the corrected Preview passed hosted smoke 27/27 plus 9/9 desktop/mobile visual owner-review checks. X008-H remains open solely for explicit owner human PASS. No Production action, merge, public signup, external-client invitation, fabricated approval/file, or workbook commit occurred.

## Owner trial blocker X009-A — same Spec 015

- [x] X009-A Diagnose and permanently fix the online "تعذر حفظ المخرج بأمان" failure without starting the visual redesign, UAT cleanup, client onboarding wizard, or any data deletion.
  - [x] X009-A-1 Reproduce and root-cause the failure on local Supabase using an authorized management UAT persona and the exact form → Zod → server action → `createDeliverableViaRpc` → `f002_create_deliverable_reservation` path.
  - [x] X009-A-2 Replace raw owner/contributor identifiers with a scoped human-readable member selector; retain safe actionable Arabic error mapping; enforce tenant/client/active-role assignment integrity in PostgreSQL without weakening RLS or the audited transactional RPC.
  - [x] X009-A-3 Add focused regression coverage for form mapping and selectors, management/account-manager directory access, client denial, invalid/cross-scope assignment denial, valid assignment, package capacity, atomic rollback, idempotent replay, audit/ledger integrity, and persistent browser creation + reload.
  - [x] X009-A-4 Pass the corrected exact-head gate matrix. CI run `29833746263` verified S015-P1-091; later run `29835323916` exposed S015-P1-092, and corrective run `29837663256` passed the complete matrix including all seven persistent scenarios and the final production build.
  - [x] X009-A-5 Isolated non-Production Preview verification under `samawahs-projects/shrik`: migration `202607210001` applied to UAT, exact-head Preview Ready, real browser/Auth creation persisted one correctly assigned deliverable with one allocation/reservation and audit evidence, then audited cancellation released the reservation and left zero active run-scoped records. Production remained untouched.

Independent review fixed S015-P1-089, the non-Production hosted create/cancel proof is green, and exact-head CI verified both S015-P1-091 and S015-P1-092. X009-A is closed. Legacy Glass package balance debt remains S015-P2-090 for the next clean-workspace checkpoint. Production remains untouched.

## Owner trial clean workspace X009-B — same Spec 015

- [x] X009-B Provision a new empty owner-entry UAT workspace, retain approved internal Samawah team identities, and quarantine legacy Hadna/Glass data without deleting audit or ledger history.
  - [x] X009-B-1 Design and document the clean-workspace model inside Spec 015 only: a new tenant inside the approved non-Production UAT, new tenant memberships + tenant-scoped role assignments for approved internal personas, zero operational data, no automatic client-persona access, and reversible legacy quarantine.
  - [x] X009-B-2 Add a pure deterministic-identifier and role-mirror contract (`src/modules/uat/clean-workspace.ts`) plus unit coverage so dry-run/apply/replay/rollback produce stable rows and never mirror client-only personas.
  - [x] X009-B-3 Add the run-ID-scoped hosted tool `scripts/prepare-s015-clean-workspace.mjs` with `--dry-run`, `--apply`, `--rollback`, and `--status`, mandatory target category + owner-approved hostname allowlist, automatic Production refusal, idempotent replay, rollback that never touches append-only history, and category/count-only output.
  - [x] X009-B-4 Add persistent browser coverage (`s015-clean-workspace-journey.spec.ts`) proving admin/account-manager/writer/designer entry, honest empty states, legacy invisibility, client-persona denial, idempotent replay, and rollback that preserves audit/ledger; add pgTAP coverage (`s015_clean_workspace_membership.test.sql`) for membership idempotency, tenant isolation, and append-only preservation.
  - [x] X009-B-5 Pass the complete local verification matrix (lint, typecheck, unit, integration, component, RLS simulator, clean Supabase reset, pgTAP, fixture E2E, persistent E2E, secret scan, diff check, build) on the exact reviewed head.
  - [x] X009-B-6 Apply the clean workspace to the approved non-Production `sharik-uat` target: guarded dry-run, apply, separate-process replay no-op, status, rollback/status rehearsal, final apply/status, and direct protected-Preview persona inspection all passed. Final clean operational counts are zero; five internal memberships are active; legacy internal memberships are inactive; legacy audit 135 and ledger 26 rows remain preserved.

Independent review fixed S015-P1-094, corrective exact-head CI run `29852802208` passed, and X009-B-6 completed on healthy non-Production `sharik-uat`. The protected Preview directly verified the empty management workspace, empty scoped team workspaces for account manager/writer/designer/unassigned, absence of Glass in their natural views, and an unaffected client viewer route without internal leakage. X009-B is closed. Production remains untouched and no merge occurred.

## Owner trial guided first-client onboarding X009-C — same Spec 015

- [x] X009-C Enable the owner to enter the first client entirely from the UI through a guided Arabic RTL wizard.
  - [x] X009-C-1 Build a combined Zod schema (`onboarding-schema.ts`) covering client + contract + package lines + first deliverable with Arabic validation rules, date-order checks, package capacity enforcement, and duplicate prevention.
  - [x] X009-C-2 Build an orchestrating server action (`onboardFirstClientAction`) that invokes one atomic audited onboarding RPC composing the existing client/contract/package/deliverable RPCs, enforces tenant scope + RLS + all four create permissions, provisions exact client-scoped roles only for selected internal team members, and uses payload-bound run-ID idempotency for safe replay.
  - [x] X009-C-3 Build a multi-step client-side wizard component (`FirstClientWizard`) with 5 input steps + review, Arabic RTL layout, mobile/desktop responsive grid, keyboard-accessible inputs, per-step validation, team member selector with human names only (no UUIDs), dynamic package-line add/remove, and a single form submission to the orchestrating action.
  - [x] X009-C-4 Wire the wizard page at `/clients/onboard` with route guards (CLIENT_CREATE + CONTRACT_CREATE + PACKAGE_CREATE + DELIVERABLE_CREATE), tenant-scoped member directory, and a stable run-ID. Add a prominent "إضافة أول عميل" CTA on the `/clients` empty state and header.
  - [x] X009-C-5 Add full test coverage: unit schema tests (17 tests), component wizard tests (7 tests), pgTAP onboarding RPC sequence + idempotency + unauthorized-role denial (15 tests), and persistent browser E2E for full success journey, unauthorized role denial, empty-field prevention, and idempotent replay (4 tests).
  - [x] X009-C-6 Pass the local verification matrix and exact-head CI: F-001 Quality run `29901885473` passed on `dc013684f51438467c7adb1dd212d9efaa510248`, including lint, typecheck, unit, integration, clean Supabase reset, RLS/pgTAP, component, fixture E2E, persistent E2E, secret scan, and build; Vercel Preview passed.
  - [x] X009-C-7 Close S015-P1-097: exact code-head F-001 run `29907779138` passed clean PostgreSQL/pgTAP 476 and persistent browser 16/16; migration `202607220001` was the only pending migration and was applied to healthy non-Production `sharik-uat`; the owner's exact partial attempt was completed forward with one audited deliverable/reservation/ledger entry; protected Preview sign-in, repaired data visibility, wizard hydration, and five eligible team members passed; hosted invalid-assignment rollback left zero rows.

## Owner trial client-ready media correction X009-D — same Spec 015

- [x] X009-D Close the image-only client-review deadlock discovered during the owner walkthrough without weakening exact-version, visibility, RLS, or audit boundaries.
  - [x] X009-D-1 Render the real current-version image/video in the unified deliverable drawer and retain a clear fallback only when no previewable current-version media exists.
  - [x] X009-D-2 Add one management-only, audited, idempotent command to stage an existing ready internal current-version file for client review while keeping it unreadable to client personas until the explicit send-to-client transition.
  - [x] X009-D-3 Make file visibility copy and the send-to-client readiness state explicit; do not label staged media as already visible before the deliverable is sent.
  - [x] X009-D-4 Prove exact-version scope, tenant/client isolation, unauthorized-role denial, pre-send client secrecy, replay safety, audit evidence, successful image-only send, and atomic promotion to final delivery with pgTAP and component/browser regressions.
  - [x] X009-D-5 Exact-head F-001 run `30214571221` passed the complete matrix on `fbee205`; additive migrations `202607260001` and `202607260002` match approved non-Production `sharik-uat`; the official protected Preview proved real 1600x1600 media, pre-send client secrecy, approver/viewer role separation, exact-version approval, delivery, final-file preview/download, and empty post-decision inbox. Production remains untouched.

## Critical workflow safety closure X010-A — same Spec 015

- [x] X010-A Close the Owner UAT P1 workflow-safety defects before any broad visual redesign.
  - [x] X010-A-1 Make upload state explicit and durable across reload/session boundaries (name, type, size, progress, success/failure/cancel/retry), prevent silent continuation while unsettled, and keep registration bound to the exact tenant/client/deliverable/current version.
  - [x] X010-A-2 Add an exact-version client-send review summary and deny pending, failed, stale, cross-deliverable, non-ready, or unregistered upload-attempt payloads.
  - [x] X010-A-3 Enforce and expose `client_approved -> ready_for_delivery -> delivered` through audited, idempotent management workflow commands.
  - [x] X010-A-4 Add an exact-version/file final-delivery confirmation and preserve single audit/ledger/package consumption on replay.
  - [x] X010-A-5 Make each deliverable list item directly openable with the Arabic action `فتح العمل`.
  - [x] X010-A-6 Correct Kanban state-to-column mapping and retain fixed-width, RTL/mobile/keyboard-accessible horizontal scrolling with protected commands outside drag/drop.
  - [x] X010-A-7 Remove numeric spinners, enforce integer validation for count quantities, preserve the existing UAT value `11.93`, and record it for owner correction through the audited adjustment path.
  - [x] X010-A-8 Pass the complete local matrix, exact-HEAD CI, correct-project Preview deployment, and bounded hosted synthetic smoke before declaring GREEN.
    - GREEN evidence: mandatory start `2f72f17`; local clean reset plus pgTAP 8/535, unit 61/279 after the fixture-tool correction, integration 28/112, component 24/88, RLS simulator 8/24, fixture E2E 126 pass/6 skips, persistent E2E 16/16, lint/typecheck/secret scan/diff/build; application HEAD `f8b363d` passed F-001 `30479343344`; correct `samawahs-projects/shrik` Preview deployment `dpl_8FsJYiCoiULPi9HJ7swZurXqZEW8` was Ready; UAT migration `202607290002` matched local/remote; shared run `s015-hosted-lifecycle-6998e30147` completed exact-version upload/reload block/cancel, internal review, two client decisions, prepare, final delivery, SLA/audit/ledger/idempotency; persona-scope run `x010a-scope-20260729-f8b363d` rolled back and replayed with zero active target client memberships/roles.
  - [x] X010-A-9 Corrective security closure for the durable-upload authorization and cancel-cleanup defects (S015-P1-111, S015-P1-112) without expanding scope.
    - Additive migration `202607300001_s015_x010a_upload_authorization_cleanup_hardening.sql` centralizes the role->visibility / `is_final` / version-state matrix in `s015_upload_actor_kind` + `s015_upload_visibility_allowed` + `s015_assert_upload_authorized`, applied at begin, retry, and complete (re-checked at completion). `cancelWorkspaceFileUpload` and `s015_cancel_file_upload_attempt` no longer trust a browser-supplied deletion path; cancel returns the attempt's true Storage coordinates and the server action inspects `remove` and reports an honest `cleanup` outcome. New pgTAP `s015_upload_authorization_hardening.test.sql` plus the corrected durable scenario prove the matrix.
    - Follow-up correction adds `202607300002_s015_x010a_cancel_authorization_followup.sql` to narrow cancel authorization itself: management may cancel authorized in-scope current-version attempts, while non-management may cancel only its own attempt and must still satisfy the upload visibility/is_final matrix. `cancelWorkspaceFileUpload` now treats missing/invalid RPC coordinates as cleanup failure, never falls back to a bucket, and both UI cancel paths warn when manual cleanup is needed. Local non-DB matrix passes (lint, typecheck, unit 62/284, integration 28/112, component 25/90, RLS simulator 8/24, secret scan, diff check, build); local pgTAP is blocked by local PostgreSQL connection failure. Commit `c95cc0cab22ade90d879000c64827365ffbb1a92` passed exact-head F-001 `30550689619`, including clean reset with migration `202607300002`, pgTAP 9/589, persistent E2E, fixture E2E, and build; Vercel and CodeRabbit are green. Hosted UAT remains pending/blocked by unavailable approved UAT credentials.
    - Closure (2026-08-04): all pending additive migrations through `202608040001` were applied to the approved non-Production UAT and inventory rechecked. Real-Auth protected-Preview role smoke passed 27/27 across desktop/mobile/RTL. Hosted lifecycle run `s015-hosted-lifecycle-c9ca31d742` passed 1/1 and ended `delivered`, including durable internal/final uploads, persisted failed-attempt cancellation, exact-version review, client changes/approval, final delivery, audit, SLA, ledger, idempotency, terminal-state denial, and client/unassigned secrecy. S015-P1-111/112 are closed; Production remains untouched.

## Owner Experience Notes Consolidation and UX Rescue X010-B — same Spec 015

Owner experience observations from the manual walkthroughs are consolidated in
`evidence/owner-manual-uat-notes-2026-07-26.md` (X010-B section) and triaged into
fixed / needs-recheck / open. X010-B stays inside Spec 015 only; it does not
reopen X010-A, does not declare GREEN for the corrective hosted UAT, and does not
invite the team. Parent disposition: X010-A-9 / S015-P1-111 / S015-P1-112 remain
`hosted-verified` after the bounded 2026-08-04 corrective lifecycle; X010-B still requires the owner's human product-acceptance decision.

- [ ] X010-B Consolidate owner experience notes and rescue the UX without creating a new Spec or parallel plan.
- [x] X010-B-0 Consolidate all owner notes into one triaged source-of-truth list and correct documentation conflicts (HEAD `47b11e9`; exact-HEAD CI green; hosted corrective UAT blocked; X010-A parent not complete while X010-A-9 open).
- [x] X010-B-1 Global density + navigation + clickability.
  - [x] Unified readable density at 100% zoom: tighten shared core tokens (card, badge/StatCard, page-header) and the three target surfaces (management dashboard, client workspace, deliverables list/board) without breaking 44px touch targets or WCAG.
  - [x] Make intended cards and rows clickable with visible focus and keyboard reach (exception-dashboard recent decisions now link to the deliverable).
  - [x] Translate every visible technical state to Arabic (raw `item.status` leak fixed in the management dashboard).
  - [x] Improve the visual hierarchy of the management dashboard, client workspace, and deliverables list.
  - [x] Fix any horizontal overflow / hidden scroll in Kanban with mouse/touch/keyboard; no page-level overflow.
  - [x] No business-workflow redesign in this slice.
  - [x] Pass exact-HEAD CI, Preview, and owner visual QA (before/after evidence).
    - B1 documented closure: `technical-green + owner-final-UAT-pending`. Exact-HEAD F-001 CI `30552777038` passed and Vercel Preview `7LXuPgu2MUb8RJNbhXigi4ZipQhK` is Ready in `samawahs-projects/shrik`. CodeRabbit showed `skipped` because the PR is a Draft (not a review outcome); no review pass is claimed. The whole recent-decision row is now a single link to the scoped deliverables page (no deep-link to a specific drawer exists yet, so the closest honest link is used). B1 is not owner-accepted; only technical-green + Preview-ready. Local matrix PASS: lint, typecheck, unit 62/284, integration 28/112, component 26/97, RLS simulator 8/24, secret scan, diff check, build. Hosted UAT remains blocked by missing approved UAT credentials; no GREEN is declared for the corrective hosted slice.
- [x] X010-B-2 Onboarding journey simplification: one obvious primary client-entry path; explicit company/contact labels; phone/WhatsApp; contract reference helper; consistent package lines; edit/resume recovery; team-assignment and quantity terminology.
  - One primary CTA «إضافة عميل جديد» on `/clients` → the unified wizard at `/clients/onboard`; the competing `/clients/new` button is removed from the header (the standalone form route remains for direct edit/access but is not the primary journey).
  - Arabic labels: «اسم الشركة أو الجهة», «اسم مسؤول التواصل», «البريد الإلكتروني», «رقم الهاتف / واتساب», «اسم العقد», «مرجع العقد — اختياري» (with helper copy explaining it is an internal code), «تاريخ بداية/نهاية العقد». No technical names or UUIDs.
  - Phone/WhatsApp: additive migration `202607310001_s015_x010b2_client_contact_phone.sql` adds `primary_contact_phone` to `public.clients` and threads it through `f001_create_client_write`, `f001_update_client_write`, and `s015_onboard_first_client` (atomic, tenant-scoped, audited, idempotent). Zod normalization + validation; phone is never stored in localStorage/logs.
  - Package: multiple services in one package with add/remove; integer-only for count units, fractional allowed for divisible units; helper copy distinguishes committed/reserved/consumed/remaining.
  - Team: «المسؤول الرئيسي عن العمل» and «أعضاء الفريق المشاركون» with Arabic role labels and helper text; exact tenant/client eligibility preserved (no permission expansion).
  - Progressive disclosure: required fields first; optional contract/package details behind a disclosure toggle; review hides empty optionals.
  - Correction/recovery: wizard state preserved within the current in-memory session after validation errors (NOT a durable draft after refresh; no PII in localStorage); the invalid field is focused; idempotency key prevents duplicate creation on resubmit; the client edit page provides a clear correction route.
  - Tests: unit (phone normalization/validation, onboarding schema phone), component (multi-service add/remove, value preservation after error, progressive disclosure, RTL/keyboard, review hides empty), pgTAP (column + CHECK constraint + recreated RPC signatures/privileges, phone persistence, cross-tenant RLS isolation, unauthorized denial, atomic onboarding with phone, idempotent replay, conflict on different phone, invalid-phone RPC rejection), persistent E2E (multi-line package + phone + edit/reload).
  - Local non-DB matrix PASS: lint, typecheck, unit 63/293, integration 28/112, component 26/97, RLS simulator 8/24, secret scan, diff check, build. DB-backed gates (pgTAP, persistent E2E) are environment-blocked locally (`LegacyDbConnectError`) and run in exact-HEAD CI. Status: `X010_B2_CORRECTIVE_IN_PROGRESS` (corrected from an inaccurate `TECHNICAL_GREEN`); the migration signature + phone-normalization defects were fixed in place, but exact-HEAD CI, DB-backed gates, hosted Preview apply of `202607310001`, and owner final UAT remain pending. X010_B2_GREEN is NOT declared.
- [x] X010-B-3 Terminology and progressive disclosure: replace hard terms ("مخرجاتي"); keep client work visible after a change request as "عاد لفريق سماوة — قيد التعديل"; hide empty optional fields with progressive disclosure.
  - Central client mapper `src/modules/deliverables/client-labels.ts` (`clientStatusLabel`/`clientVisibleStatusLabel`/`clientNextAction`/`clientWorkSections`); raw status enum carried on `ClientApprovalPanelItem.status` and `ClientSafeDeliverableDetail.status`; scattered inline maps in `commercial-summary.tsx`, `persistent-client-approval.ts`, `client-deliverable-detail.tsx`, and fixture strings consolidated.
  - New `/client/work` «أعمالي» page + `ClientWorkBoard` (five role-aware sections, clickable keyboard `<Link>` cards: name/type/Arabic status/date/progress/next action); «أعمالي» added to client shell + navigation resolver.
  - Change-request visibility root-cause fix: `client_changes_requested` added to `clientVisibleStatuses` in `commercial-summary-read.ts`; change-requested work stays in أعمالي with «استلم فريق سماوة ملاحظاتك، والعمل الآن قيد التعديل» and is removed from بانتظار موافقتي; no internal comments/files/versions/quality exposed. _(Superseded — the approximate `lastDecisionAt` from `deliverable.updated_at` is no longer shown; see the corrective pass below. No approximate decision date is rendered until a scoped `approval_decisions` read is verified.)_
  - Client home: بانتظار موافقتي / أعمالي / الباقة والمتبقي are clickable `ButtonLink` cards with CTAs and useful empty states.
  - Progressive disclosure in the deliverable create/edit form: essential fields first (اسم العمل/النوع/المسؤول/الموعد/الوصف/سطر الباقة+الكمية); optional fields (الأولوية/المساهمون/تواريخ إضافية/أعلام الاعتماد) behind a native `<details>`; no field/data/validation/workflow removed; values preserved on open/close.
  - Terminology sweep: «المخرجات»/«مخرجاتي» → «الأعمال»/«أعمالي» on client surfaces and shared snapshot cards; no raw enum/UUID/internal term reaches the client.
  - Regressions: `tests/unit/deliverables/client-labels.test.ts`; `tests/component/client/client-work-board.test.tsx`; updated `client-pending-inbox`, `client-onboarding`, `r007-client-approval-panel`, `deliverable-form`, `navigation-resolver`, `hadna-mvp-summary` tests; new `tests/e2e/client/client-work-experience.spec.ts` (grouping + keyboard cards, change-request stays visible / not in pending, viewer neutrality, no enum/UUID/internal leak, clickable home sections, mobile viewport).
  - Permissions unchanged: viewer read-only; approver decides only on exact awaiting version; no internal leak; Client A ≠ Client B (RLS/tenant scope untouched).
  - Local non-DB matrix PASS: lint; typecheck; unit 64/303; integration 28/112; component 27/105; RLS simulator 8/24; fixture E2E 144 passed / 8 skipped (1 flaky mobile case passed on re-run); secret scan; `git diff --check`; build (`/client/work` present). DB-backed gates (pgTAP, persistent E2E) environment-blocked locally (`LegacyDbConnectError`) → run in exact-HEAD CI; not converted to PASS.
  - Status: `X010_B3_CORRECTIVE_LOCAL_COMPLETE_CI_UAT_PENDING`. GREEN / TEAM_UAT_READY NOT declared. No push/deploy/Production/hosted-migration/merge/invitation. X010-A-9 / S015-P1-111 / S015-P1-112 remain `code-fixed + CI-green + hosted-blocked`.
  - Corrective pass (from B3 HEAD `351f370`): (1) removed approximate `lastDecisionAt` (`deliverable.updated_at`) — decision date **deferred** until a scoped `approval_decisions` query + client RLS read verification is added; no approximate dates shown. (2) New real work detail route `/client/work/[deliverableId]` + `readPersistentClientWorkDetail` (tenant/client guard + RLS; client-visible statuses only; client-visible version/files/comments only; no internal data; approver decision buttons only at `waiting_client_approval`; viewer read-only; deliverable ID in href/key only). (3) Role-aware next-action copy; viewer never sees «بانتظار قرارك» (uses «قيد المراجعة»); «تم اعتمادك» → «تم اعتماد العمل». (4) Form split into البيانات الأساسية / تفاصيل إضافية اختيارية / إعدادات سير العمل (approval flags now a visible fieldset with impact explanations). (5) `/client/work` distinguishes read failure (ErrorState) from empty (EmptyState). Local non-DB matrix PASS (unit 64/303, integration 28/112, component 27/106, RLS simulator 8/24, fixture E2E 154 passed / 8 skipped, secret scan, diff check, build with both routes); DB-backed gates environment-blocked → CI.
  - B3 RLS/PostgreSQL corrective (additive migration `202608010001_s015_x010b3_client_change_request_visibility.sql`): redefined `s015_client_current_version_is_visible` to add only `client_changes_requested` to the visible deliverable-status set, preserving the `auth.uid()` guard, tenant/client role checks, uniform-false behavior, exact-current-version matching, security definer, `search_path`, grants/revokes; recreated the `f006 client portal select deliverables` policy with the same membership/role/current-version/helper checks plus the one added status; version/comment/file policies already call the helper and were not widened. Corrected `clientVisibleVersionStatuses` in `persistent-client-approval.ts` to version-only statuses (`client_visible`, `client_approved`, `final`) — removed the deliverable statuses (`waiting_client_approval`, `client_changes_requested`, `delivered`) that were never version statuses. Added UUID validation in the persistent work-detail route so an invalid ID returns a safe Not Found without sending a bad uuid cast to PostgreSQL; fixture IDs stay allowed only in fixture mode. Fixed the Client A/B isolation test: a valid `client_approver_b` persona assigned to `client_b` now tries to open a Client A deliverable ID and receives ResourceNotFound with zero Client A data leak (the previous «لا يوجد عملاء مسندون» result did not prove isolation). Added mandatory pgTAP `s015_x010b3_client_change_request_visibility.test.sql`. Reverted `next-env.d.ts` to `./.next/types/routes.d.ts`. Corrected the tasks.md `lastDecisionAt` claim (superseded). Status: `X010_B3_RLS_CORRECTIVE_LOCAL_CODE_COMPLETE_DB_CI_UAT_PENDING`. GREEN NOT declared; DB-backed gates remain environment-blocked locally and run in exact-HEAD CI.
- [x] X010-B-4 Understandable in-app notifications center (approval / change-request); no email integration without owner approval.
  - Plan (recorded before implementation): persistent, person-scoped, deduped in-app notifications only; no email/whatsapp/push/cron in this slice. Additive migration `202608010002_s015_x010b4_in_app_notifications.sql` after `202608010001` introduces `public.notifications` (id, tenant_id, client_id nullable, recipient_user_id, event_type, title, message, action_href, source_audit_event_id, dedupe_key, read_at, created_at) with RLS (recipient + active-tenant-member + client-scope defense-in-depth), a CHECK-enforced action_href allowlist, a read-only-except-read_at guard, and no authenticated INSERT/DELETE. Notification emission is atomic with the existing audited workflow: a SECURITY DEFINER `AFTER INSERT` trigger on `public.audit_events` fans out the core events (`DeliverableVersionSubmitted`, `DeliverableInternalChangesRequested`, `DeliverableVersionSentToClient`, `ClientVersionDecision` approved/changes_requested, `DeliverablePreparedForDelivery`, `DeliverableFinalDelivered`) and a SECURITY DEFINER `AFTER INSERT/UPDATE` trigger on `public.deliverable_tasks` covers assignment/reassignment; both call a shared `s015_enqueue_notification` with `ON CONFLICT (recipient_user_id, dedupe_key) DO NOTHING`. The actor is never a recipient unless follow-up is genuinely required. Scoped RPCs (`s015_notification_unread_count`, `s015_list_notifications`, `s015_mark_notification_read`, `s015_mark_all_notifications_read`) are the only read/mark paths; no service role in browser/runtime. UI: a bell with unread badge + popover and a unified `/notifications` route with All/Unread filters, Arabic human copy, loading/error/empty states, keyboard + RTL + mobile, wired into ProductShell (management/team) and ClientShell (client). Client notifications carry only client-safe text and safe hrefs; no internal data leak. Events covered: assignment/reassignment → assignee; submit version → management; internal change request → owner + execution contributors; send to client → client approver/viewer by permission; client change request → management + account manager + assigned team; client approval → management + account manager; prepare/complete delivery → relevant internal roles + client (final). Email notifications are deferred to a separate owner decision (S015-P2-127); X010-B4 is in-app only.
  - Implemented (status `X010_B4_LOCAL_COMPLETE_DB_CI_UAT_PENDING`). Started from mandatory HEAD `fc5b414`. Migration `202608010002` is additive and does not edit any historical migration file. Local non-DB matrix PASS: lint; typecheck; unit 66/333 (incl. new `notification-labels` + `notifications-read` mapper tests); integration 28/112; component 29/119 (incl. new `notification-bell` + `notification-list` tests); RLS simulator 8/24; fixture E2E 12/12 new (`notifications-center.spec.ts`) plus 17/17 regression across app-shell/client-work/pending-inbox/visual-qa; secret scan; `git diff --check` (LF/CRLF warnings only); production build (`/notifications` route present). DB-backed gates (pgTAP `s015_x010b4_in_app_notifications.test.sql`, persistent `s015-notifications-journey.spec.ts`) environment-blocked locally (`LegacyDbConnectError`, Docker daemon down) → run in exact-HEAD CI; **not** converted to PASS. GREEN / TEAM_UAT_READY NOT declared. No push/deploy/Production/hosted-migration/merge/invitation.
  - Corrective close (status `X010_B4_CORRECTIVE_LOCAL_COMPLETE_DB_CI_UAT_PENDING`): independent review registered S015-P1-129/130/131 and S015-P2-129/130. Fixed the candidate-recipient resolver so it evaluates each active client member + role instead of the actor; added one current-user access predicate to RLS and every SECURITY DEFINER list/count/mark RPC so revoked Client A scope cannot read or mutate old Client A notifications; corrected an invalid `END IF` loop terminator that would have blocked migration apply; split viewer/approver copy and management/execution action routes; made task reassignment dedupe include the persisted mutation timestamp; stabilized relative-time hydration using one server render snapshot. Expanded pgTAP for no-role recipients, Client A/B RPC isolation, post-revocation denial, repeated reassignment, role copy, and role-safe routes. Local proof after correction: lint PASS; typecheck PASS; unit 66/333; integration 28/112; component 29/119 plus focused notification components 13/13; RLS simulator 8/24; notification E2E 12/12 before hydration correction and desktop 4/4 after correction with no hydration warning. pgTAP/persistent E2E remain environment-blocked (`LegacyDbConnectError`) and exact-HEAD CI/Preview/owner UAT remain pending; no GREEN claim and no push/deploy/Production/hosted migration/merge/invitation.
  - **Exact-HEAD CI GREEN (status `X010_B4_CI_GREEN_UAT_PENDING`, final HEAD `b4726cf` on `codex/015-persistent-mvp-pilot-completion`).** The first exact-HEAD run on the consolidated B2/B3/B4 head exposed pre-existing defects that had never reached CI (B2/B3 were local-complete only); all were root-caused and fixed inside Spec 015 without weakening any expectation. F-001 Quality run `30741184814` PASS: lint, typecheck, unit 66/333, integration 28/112, RLS simulator 8/24, **pgTAP 12 files / 673 tests** (incl. `s015_x010b4_in_app_notifications`), component 29/119, fixture E2E, **persistent E2E 18/18** (incl. `s015-notifications-journey` + `s015-deliverable-creation-journey`), secret scan, build. Vercel `5fEXUfcYWVJREfMFE2eHmATJ8Fpd` Ready in `samawahs-projects/shrik`; CodeRabbit SUCCESS. CI proves migrations apply through `202608010002`, the full notifications pgTAP, the persistent notifications journey, tenant/client isolation, post-revocation list/count/mark denial, and viewer/approver recipient behavior. Additional CI-hardening fixes: a1r legacy 8-param `f001_update_client_write` call sites; B2 test expectations + a distinct cross-tenant 42501 in `202607310001`; B4 review-payload fixture caption; `service_role` SELECT grant on `public.notifications` (`202608010002`); persistent creation journey opens the B3 progressive-disclosure `<details>`; persistent notifications journey uses the proven writer-submits + management-approves-and-sends `workflowStep` flow. **GREEN / TEAM_UAT_READY / Hosted UAT NOT declared.** X010-A-9 / S015-P1-111 / S015-P1-112 remain `code-fixed + CI-green + hosted-blocked`.
- [x] X010-B-5 Files as a Drive-like experience (folders/classification/previews/Arabic names); simplify "تنزيل آمن" to "تنزيل"; refine upload progress/failure/retry/cancel clarity.
  - Implemented (status `X010_B5_CI_GREEN_UAT_PENDING`, final HEAD `e47c810` on `codex/015-persistent-mvp-pilot-completion`). Bounded to Spec 015 only; no new Spec/ADR/dependency/migration. No workflow, RLS, permission, or audit change — the file read paths keep their existing tenant/client scope and visibility filters; B5 is presentation only. Exact-HEAD F-001 run `30743491402` PASS: lint, typecheck, unit 67/342, integration 28/112, RLS simulator 8/24, **pgTAP 12/673**, component 30/125, **fixture E2E 178** (incl. `client-files-experience`), **persistent E2E 18/18**, secret scan, build. Vercel `2JGvwkkjYc4QRfH6JVFUeeN5aMSR` Ready; CodeRabbit SUCCESS. **GREEN / TEAM_UAT_READY NOT declared.**
  - Client files (`/client/files`): retitled «ملفاتي» with a short description; rebuilt as grouped folders from existing metadata — التسليمات النهائية / ملفات للمراجعة / ملفات رفعتها / العقد والهوية — each with a counter, a useful empty state, and empty groups hidden. Cards show type/size/date/related-work/version-status with empty fields hidden; real image thumbnails, safe inline video/PDF preview, and an honest fallback for non-previewable types; the whole card is keyboard-openable (`tabindex=0`, Enter/Space) and the «تنزيل» button uses a short-lived server-signed URL. No Storage/bucket/UUID/visibility-enum text reaches the client. Pure grouping helpers in `src/modules/files/file-groups.ts`; board in `src/ui/client/client-files-board.tsx`.
  - Team/admin (universal drawer): files grouped by visibility context — داخلي لفريق سماوة / مرسل للعميل / رفع العميل / تسليم نهائي — with per-group counts, while the existing internal-secrecy, final-delivery authorization, and stage-for-client controls are unchanged (writer/designer still cannot declare a final delivery).
  - Upload UX: Arabic states refined to «جارٍ الرفع» / «اكتمل الرفع» / «تعذر الرفع» / «أُلغي» with an explicit cancelled row state; retry + audited cancel preserved; X010-A durable-upload behavior (no silent replacement, no success before PostgreSQL+Storage registration) untouched. «تنزيل آمن» → «تنزيل» everywhere (download button + preview fallback).
  - Tests: unit `tests/unit/files/file-groups.test.ts` (grouping/labels/size/preview/status-no-leak); component `tests/component/client/client-files-board.test.tsx` (folders, counts, empty state, no enum/UUID/storage leak, «تنزيل» only, signed-download click, keyboard-openable); fixture E2E `tests/e2e/client/client-files-experience.spec.ts` (desktop/mobile/RTL/keyboard + Client A≠B). File isolation/visibility/download remain covered by the existing pgTAP (`s015_persistent_mvp_behavior`, `s015_upload_authorization_hardening`); the deliverable-name join inherits `deliverables` RLS.
  - Local non-DB matrix PASS: lint; typecheck; unit 67/342; integration 28/112; component 30/125; RLS simulator 8/24; secret scan; `git diff --check` (LF/CRLF warnings only); production build; client-files E2E 3 passed / 1 mobile skip. DB-backed gates (pgTAP, persistent E2E) environment-blocked locally (`LegacyDbConnectError`, Docker down) → exact-HEAD CI; **not** converted to PASS.
  - Boundary: no push-beyond-this-branch/deploy/Production/hosted-migration/merge/invitation. **GREEN / TEAM_UAT_READY NOT declared.** X010-A-9 / S015-P1-111 / S015-P1-112 remain `code-fixed + CI-green + hosted-blocked`.
  - Corrective CI close (status `X010_B5_CORRECTIVE_CI_GREEN_OWNER_UAT_PENDING`, final corrective code HEAD `8e5c3bd4a86da05dd7411d4352bac3c5f4fe614e`, documentation follow-up HEAD `bbfe58ea22ee3297d9ab70524019e15d93d8ddce`): bounded corrective commits on top of mandatory ancestor `59c25eb` fixed the contract/identity folder permission mismatch, honest read-failure UI, preview-only card interactivity, lazy image-only preview signing, DOM raw visibility/UUID leakage, and preview modal accessibility. Additive migration `202608020001_s015_x010b5_client_document_files.sql` extends client read/download authorization only for active same-tenant/same-client `contract_file`, `report_file`, and `brand_asset` files, while keeping `internal_only` hidden, Client A/B isolated, and disabled membership denied. Exact corrective code F-001 run `30755205688` SUCCESS: lint, typecheck, unit 67/342, integration 28/112, clean reset through `202608020001`, RLS simulator 8/24, pgTAP 13/682 including `s015_x010b5_client_document_files`, component 30/126, fixture E2E 178, persistent E2E 18/18, secret scan, and build. Documentation follow-up F-001 run `30796391833` SUCCESS; CodeRabbit SUCCESS; Vercel Preview `dpl_8afgdFgy9NcSTGEeSAkJwUWLg3xy` Ready at `https://shrik-nzrv6pwrc-samawahs-projects.vercel.app`. S015-P2-120 = `technical-fixed; CI-verified; final-owner-UAT-pending`. No merge, Production, hosted migration, Hosted UAT mutation, or B6 start.
- [ ] X010-B-6 Drawer reorganization (logical sections/tabs); internal-quality explanation and a default editable checklist; owner/contributor/role display clarity with no `tenant_administrator` or synthetic-data leak; visual polish for client profile, admin dashboard, and team dashboard.
  - [x] X010-B-6A CI green (2026-08-03): implemented only S015-P2-121, S015-P2-122, S015-P2-123, and the visual-density part of D18. The drawer is tabbed into overview/content/files/execution/comments/internal-quality/activity; overview shows only status, progress, due date, owner, and next action; forms stay mounted so values survive tab navigation. Internal quality is explained as Samawah-only, starts from an editable default checklist, and saves only through the existing quality command/RPC after an explicit action. Member display now uses human `member_profiles` names, Arabic role labels, and `عضو فريق` fallback instead of UUID/raw role/synthetic email. Visual density was tightened only on the drawer, client shell/home profile area, management dashboard, and team workspace. Local verification PASS: lint; typecheck; unit 69/346; integration 28/112; component 30/128; RLS simulator 8/24; fixture E2E 179 passed / 10 skipped; persistent E2E 18/18 against local Supabase; secret scan; `git diff --check` (CRLF warnings only); build. Remote verification PASS on code HEAD `4629b9362d5ad5c59b94afb141dd360f4cc9ed45`: F-001 run `30825492962`; CodeRabbit SUCCESS; Vercel Preview Ready at `https://shrik-git-codex-015-persistent-mvp-pil-cbe689-samawahs-projects.vercel.app`. Owner UAT / TEAM_UAT_READY not declared.
  - [x] X010-B-6A corrective CI gate (2026-08-03): S015-P1-132 and S015-P2-131 fixed and CI-verified. Default quality checklist save is one additive PostgreSQL RPC (`202608030001`) with atomic rollback including audit and stable idempotent replay; Drawer load/refresh failures are honest and retryable; fixture mode returns a bounded real workspace and rejects unknown fixtures. Local reset + pgTAP 14/695, lint/typecheck, unit 70/349, integration 28/112, component 30/130, RLS simulator 8/24, build, secret scan, and targeted desktop/mobile/RTL visual Drawer E2E PASS. Exact code HEAD `b825182` passed F-001 `30837530949`, including fixture E2E 178/10 configured skips and persistent E2E 18/18; Vercel passed on `samawahs-projects/shrik`. Status `X010_B6A_CORRECTIVE_CI_GREEN_OWNER_UAT_PENDING`; Owner UAT remains open.
  - [x] X010-B-6B technical + exact-HEAD CI closure (2026-08-04): S015-P2-124/125/126 are technically fixed and verified. Safe pre-client internal rework is audited/idempotent and exact-version/revision bound; silent recall after client exposure is forbidden in V1 and the audited client change-request path remains the only supported return. Kanban drag is limited to ordinary `not_started`/`in_progress` movement; protected states use explicit actions. Team invitations now provide one-time-link create/resend/revoke/read/accept, exact-email Auth acceptance, exact client-scoped roles, human member directory, audit, and idempotent replay; superseded RPCs are non-executable and disabled memberships cannot be silently reactivated. Final corrective HEAD `d90f606` passed F-001 `30911193680`: lint; typecheck; unit 71/353; integration 28/112; component 30/133; RLS simulator 8/24; clean reset; pgTAP 15/729; fixture E2E 178 passed / 10 configured skips; persistent E2E 19/19; build; secret scan. No Owner PASS / TEAM_UAT_READY / Production / merge / real invitations / email.
- [ ] X010-B-7 Final structured owner acceptance trial after B2–B6: bounded owner walkthrough of the simplified onboarding, terminology, notifications, files, and drawer experience on the corrected Preview; close only on explicit owner PASS. No Production, merge, or team invitation before owner PASS.
  - [x] X010-B-7A Technical preparation: approved UAT migrations through `202608040001` applied; protected Preview route/persona smoke 27/27; full hosted lifecycle 1/1 (`s015-hosted-lifecycle-c9ca31d742`); one persistent owner-review item prepared as `s015-owner-trial-e54d621cfe`; no Production action.
  - [ ] X010-B-7B Owner human walkthrough: execute `evidence/owner-acceptance-walkthrough-ar.md` and record explicit PASS/FAIL for management, assigned team, client viewer, and client approver. This is the only remaining B7 close action.
    - Owner disposition (2026-09-01): the walkthrough is intentionally deferred while the newly observed P1/P2 defect and UX burn-down proceeds. This is authorization to implement corrections only; it is not PASS or a waiver. All unchecked owner-acceptance checklist items remain open and every future agent must preserve the blocked readiness/Production/invitation boundary.
  - [ ] X010-B-7C Burn down the 2026-09-01 continuation findings in `evidence/owner-manual-uat-notes-2026-07-26.md`, one regression-backed slice at a time, starting with S015-P1-133 notification read/list consistency and then the owner-rejected shared UI density/navigation surfaces.
    - [x] X010-B-7C-1 S015-P1-133 local correction: accept PostgreSQL timezone-offset `read_at`, keep the unread count while presenting an honest recent-list error, add unit/component regressions, and extend the persistent notification journey to mark/reload/assert the real row. Local lint/typecheck, unit 71/355, integration 28/112, focused component 14/14, notification fixture E2E 12/12 (desktop/mobile/RTL), diff check, and production build pass. The persistent regression is added but its local run correctly refused the current non-local Supabase URL; exact-HEAD CI/local-Supabase execution and owner Preview recheck remain pending.
    - [x] X010-B-7C-2 Correct the shared product-shell foundation for S015-P2-132/S015-P2-136: compact dark navigation rail, wider quieter content canvas, reduced shell whitespace, and Arabic route labels for notifications/onboarding. Preserved role-aware navigation, mobile horizontal navigation, 44px targets, focus, and RTL. Local lint/typecheck, component 30/136, focused shell tests 14/14, and visual fixture E2E 15/15 across desktop/mobile/RTL pass; local screenshots were inspected. This is the shared foundation only—surface/card/drawer density and remaining English copy stay open.
      - [x] X010-B-7C-3 Correct S015-P1-134 at the owner-trial boundary: direct UAT diagnosis proved `s015-owner-trial-e54d621cfe` has a final text version but zero authorized `file_assets`; the product file read/preview path and full hosted lifecycle already pass with real final files. The owner-trial preparation now creates an audited durable-upload attempt, uploads a synthetic PNG as the assigned writer, atomically registers it on the exact current version, approves internally, stages that exact file for client review, and verifies viewer/approver image visibility plus download before leaving the item at `waiting_client_approval`. The hosted test transforms/lists 3 projects without mutation. Existing UAT data was not fabricated or mutated in this local slice; a new owner-trial run and owner recheck remain pending after deployment.
      - [x] X010-B-7C-4 Correct S015-P1-135 locally: introduced explicit `PERM.DELIVERABLE.VIEW` for account manager/content writer/designer, replaced the erroneous contract-view gate on client deliverables, and lets the client workspace derive non-commercial counts from only the deliverables returned by existing RLS. For writer/designer, the misleading «الباقة 0 بنود» card is replaced by a scoped completion count; contract/package/ledger links and balances remain denied. Local lint/typecheck, unit 71/355, integration 28/112, component 30/137, RLS simulator 8/24, assigned-writer desktop browser 1/1, secret scan, diff check, and production build pass. Exact-HEAD CI, real-Auth persistent/Preview, and owner recheck remain pending.
    - [x] X010-B-7C-5 Second owner-feedback correction batch: complete the shared UX work for S015-P2-133/S015-P2-134 and the remaining visible part of S015-P2-136 without changing business workflow, RLS, or permissions. Local technical correction is complete; exact-HEAD CI, corrected Preview, and owner recheck remain pending.
      - [x] X010-B-7C-5A Make the primary destination of management client cards, client-workspace path cards, and assigned-client cards reachable from the whole card with one semantic link and visible keyboard focus; keep secondary actions as separate links and do not create nested interactive elements.
      - [x] X010-B-7C-5B Replace the universal drawer's horizontally scrolling tab strip with a responsive, sticky, wrapping/grid tablist; preserve RTL arrow/Home/End keyboard behavior, focus return, unsaved form values, 44px targets, and role-filtered sections.
      - [x] X010-B-7C-5C Localize visible content-format/channel labels such as `Post` and use one safe Arabic date/date-time formatter on the corrected drawer/content-card surfaces; invalid or unknown technical values must fall back to calm Arabic copy rather than leak raw enums.
      - [x] X010-B-7C-5D Add unit/component/browser regressions, visually inspect desktop/mobile/RTL fixture output, update defect/evidence/progress files, and leave CI/Preview/owner acceptance explicitly pending. Local PASS: lint; typecheck; unit 72 files / 358 tests; component 31 files / 139 tests; whole-card management + assigned-team browser 2/2; Drawer visual browser 3/3 across desktop/mobile/RTL; production build; secret scan; diff check. The first parallel full-test attempt exposed one obsolete English assertion and worker resource timeouts; the assertion was corrected and both suites passed serially. No hosted mutation, invitation, merge, or Production action.
    - [x] X010-B-7C-6 Third owner-feedback correction batch: restore commercial trust for S015-P2-135 without mutating or deleting UAT records and without changing ledger, workflow, RLS, or permissions.
      - [x] X010-B-7C-6A Use one tested commercial presentation model for count-unit integrity, delivered/reserved/remaining quantities, and Arabic Gregorian dates; a negative balance or fractional count must be flagged as requiring audited correction rather than displayed as a valid remaining count.
      - [x] X010-B-7C-6B Align management/client commercial summaries with the same scoped contract/package/deliverable rows, expose delivered quantity, and render period ranges in an unambiguous Arabic RTL order.
      - [x] X010-B-7C-6C Make management contract/package lists compact, searchable, status-filterable, and bounded by pagination; quarantine known run-scoped hosted contracts from normal human-trial lists using the existing visibility rule, while preserving their database/audit/ledger history.
      - [x] X010-B-7C-6D Add unit/component/browser regressions for integrity warnings, delivered quantity, search/filter/pagination, no raw synthetic marker, and desktop/mobile/RTL presentation; update evidence and leave exact-HEAD CI, corrected Preview, safe UAT cleanup, and owner acceptance pending. Local PASS: lint plus exact-source targeted ESLint; typecheck; unit 73 files / 362 tests; component 31 files / 142 tests; integration 28 files / 112 tests; commercial browser 9/9 across desktop/mobile/RTL plus a final mobile overflow regression 1/1; manual desktop/mobile screenshot inspection; production build; secret scan; diff check. Visual QA found and corrected a 541px client-shell document on a 390px viewport; the final measured document width is 390px. No migration, dependency, ADR, ledger/workflow/RLS/permission change, hosted mutation, UAT deletion, invitation, merge, or Production action.
    - [ ] X010-B-7C-7 Fourth owner-feedback correction batch: correct S015-P2-137 team/invitation UX while preserving tenant/client isolation, exact-email acceptance, audit, and idempotency. Local code and non-DB verification are complete; PostgreSQL/pgTAP, exact-HEAD CI, corrected Preview, and owner acceptance remain pending.
      - [x] X010-B-7C-7A Require explicit role and active-client selection; support one or more authorized client scopes in one invitation and explain the resulting access before creation.
      - [x] X010-B-7C-7B Keep pending invitations actionable, remove accepted-member duplication, and place revoked/superseded records in a compact closed-history disclosure.
      - [ ] X010-B-7C-7C Extend the audited invitation RPC/read/accept path to exact multi-client scope without widening roles or reactivating disabled memberships; add PostgreSQL and server-action regressions. Implementation and pgTAP coverage are present, but local PostgreSQL verification was blocked because Docker Desktop 4.79.0 crashes while recreating its stale `dockerInference` runtime socket after the power outage, so CI remains mandatory. Update 2026-09-03 (outside-batch evidence, no closure): Docker recovered without destructive action and the local pgTAP matrix now passes 16 files / 744 tests including `s015_x010b7c7_multi_client_team_invitations.test.sql`; the task stays open pending exact-HEAD CI, hosted migration application, and owner recheck.
      - [x] X010-B-7C-7D Add component/browser coverage for explicit defaults, multi-client selection, compact lifecycle presentation, Arabic RTL/mobile/keyboard layout, and no raw identifier/client-scope leakage; update evidence and leave CI/Preview/owner acceptance pending. Local PASS: lint; typecheck; unit 73 files / 364 tests; component 31 files / 144 tests; integration 28 files / 112 tests; RLS simulator 8 files / 24 tests; invitation browser 6/6 across desktop/mobile/Arabic RTL plus final visual refresh 3/3; manual desktop/mobile screenshot inspection; production build; secret scan; diff check. PostgreSQL/pgTAP, exact-HEAD CI, migration application, corrected Preview, and all 20 owner checks remain pending.
    - [ ] X010-B-7C-8 Fifth owner-feedback correction batch: correct S015-P2-138 onboarding review and field validation UX without changing workflow, RLS, permissions, audit, or the data model.
      - [x] X010-B-7C-8A Present an organized final review before submission: client details, contract name/reference/period, package services with quantities and units, the primary owner and every contributor by human name and Arabic role, and the first deliverable's description/type/priority, all provided dates through one Arabic Gregorian formatter, and the internal/client approval settings; omit empty optional values and never render raw IDs or enum values.
      - [x] X010-B-7C-8B Replace generic step errors with field-addressable validation: deterministic focus on the first actually-invalid field (company name before the optional phone), inline per-field messages with stable aria-invalid/aria-describedby wiring, per-field error clearing, and preserved entered values with no journey reset.
      - [x] X010-B-7C-8C Add component regressions (empty-submit focus, inline error, aria wiring, phone neutrality, review completeness, optional-value hiding) and a fixture browser onboarding-review UX spec covering the full review journey, keyboard focus, RTL, desktop/mobile, no unintended horizontal overflow, screen-reader-reachable errors, and back/forward data preservation; extend the persistent onboarding journey with focus/inline-error assertions.
      - [x] X010-B-7C-8D Update the defect register, execution log, gate status, owner UAT notes, and project progress; record local persistent/pgTAP execution status honestly and leave exact-HEAD CI, corrected Preview, and all owner acceptance checks pending.
        - Evidence (2026-09-03): `first-client-wizard.tsx` review rebuilt into compact client/contract/package/team/deliverable sections (owner + contributors by human name and Arabic role, all provided dates via `formatArabicDate`/`formatArabicDateRange`, «نعم/لا» approval settings, empty optionals omitted, safe Arabic fallbacks instead of raw enums); step validation rewritten as `WizardFieldError { field, message }` with `data-wizard-field` focus binding, per-field inline `<span id="onboarding-error-{field}">`, `aria-invalid`/`aria-describedby`, per-field clearing, and an aria-live summary that never replaces the field error. Independent review found and corrected one hidden-field edge case: an invalid reversed package period now reopens its collapsed optional disclosure before focusing the exact end-date error. Independent rerun PASS: lint; typecheck; unit 73 files / 364 tests; component 31 files / 152 tests (wizard 20/20); fixture browser `onboarding-review-ux.spec.ts` 10 passed / 2 mobile-only skips across desktop, mobile, and Arabic RTL after one web-server warm-up abort and a successful retry. Earlier same-batch evidence remains: integration 28/112; RLS simulator 8/24; pgTAP 16/744 after Docker recovered; persistent onboarding journey 4/4; production build; secret scan; diff check. Exact-HEAD CI, corrected Preview, hosted migration state (none needed — no migration in this batch), and all 20 owner checks remain pending; X010-B-7C-7C/parent batches stay governed by their own closure rules.
    - [ ] X010-B-7C-9 Sixth owner-feedback correction batch: resolve S015-P2-128/S015-P2-139 through a reversible owner-trial workspace rollover that preserves every source operational, file, audit, and ledger row; do not implement destructive record cleanup.
      - [x] X010-B-7C-9A Inventory the existing X009-B clean-workspace contract and add failing tests for the current defect: each approved internal persona may have multiple historical inactive memberships, but exactly one shared active source membership must be selected; zero or multiple active sources, mismatched source tenants, client-only roles, or an ambiguous target must fail closed.
      - [x] X010-B-7C-9B Harden `src/modules/uat/clean-workspace.ts` and `scripts/prepare-s015-clean-workspace.mjs` without a parallel cleanup path: preserve deterministic run-scoped identifiers and guarded `--dry-run`/`--status`/`--apply`/`--rollback`; fully provision and verify the empty target; persist and conflict-check a deterministic append-only binding to the exact source tenant/membership set before disabling it; make replay a no-op; compensate partial apply safely; and make later-process rollback restore only the bound source while disabling only the same-run target.
      - [x] X010-B-7C-9C Prove with unit, pgTAP, and persistent browser coverage that historical inactive workspaces do not cause ambiguity, management/approved internal roles land naturally in an empty target, client personas receive no automatic access, source data and Storage metadata are untouched, `audit_events` and `package_ledger_entries` remain append-only, and apply/replay/rollback remain tenant-isolated and idempotent.
      - [x] X010-B-7C-9D Run the scoped local quality matrix, inspect Arabic RTL empty states where affected, and update Spec 015 evidence with exact counts and honest blockers. Do not apply against hosted UAT, deploy, invite users, merge, or touch Production in this implementation slice.
        - Evidence (2026-09-03, local only): reproduced the second-rollover defect as failing unit tests first (11 failures against the old status-blind selector), then replaced it with `planCleanWorkspaceSourceSelection` (exactly one active source per persona; historical inactive tolerated; zero/multiple active, cross-persona tenant mismatch, and target collision fail closed) plus a deterministic append-only binding representation (`x010b7c9_clean_workspace_source_binding`, canonical `run_id/source_tenant/source_memberships` reason, build/parse/match helpers) and a synchronization-guard test pinning the hosted script to the mirrored identifiers, format, and fail-closed codes. The hosted tool now resolves the binding in every mode: apply provisions and verifies the empty target (5 active memberships, ≥1 tenant-scoped role each, zero operational rows) before writing the binding and only then disabling the exact source set; already-applied replay is a verified no-op; sources-active replay (post-rollback) must re-select exactly the bound set or fail `CLEAN_WORKSPACE_BINDING_CONFLICT`; missing/partial binding identity fails closed; rollback requires the binding, reactivates only its recorded ids, and disables only the same-run target; dry-run/status/apply/rollback flags, hostname allowlisting, non-Production refusal, confirmations, and category/count-only output are retained; compensation keeps the last verified active workspace. No migration, dependency, ADR, workflow, RLS, tenancy, or schema change; no business/file/audit/ledger deletion. Local PASS: focused clean-workspace unit 21/21 (written failing first); full unit 73 files / 373 tests; typecheck; lint; RLS simulator 8/24; local `supabase db reset --local --no-seed` + full pgTAP 16 files / 755 tests (11 new assertions for multi-history tolerance, binding-exact rollback, and append-only preservation); persistent clean-workspace journey 10/10 against real local Supabase — apply/binding, roles entry, client-persona denial, no-op replay, rollback, re-apply-after-rollback, second rollover over two historical inactive workspaces per persona, binding-exact second rollback, ambiguity denial without mutation, and swapped-source tenant-mismatch denial without mutation; production build; secret scan; `git diff --check` exit 0. Two earlier journey runs exposed and fixed two overly-broad test assertions (client personas legitimately stay active in the source tenant). Exact-HEAD CI, hosted rehearsal/apply/rollback (X010-B-7C-9E), corrected Preview persona verification, and owner recheck remain pending.
      - [ ] X010-B-7C-9E Owner-authorized hosted rehearsal and apply: only after reviewed exact-HEAD CI and explicit target confirmation, execute redacted dry-run, apply, separate-process replay/status, rollback/status rehearsal, final apply/status, and protected Preview persona verification. This remains pending and is not delegated by the local implementation prompt.
  - Owner procedure: `evidence/owner-acceptance-walkthrough-ar.md`.
  - [x] X010-B-7C-10 Local S015-P2-136 client-detail/file-date display sweep: implementation and lead four-file diff review complete; reused the Arabic Gregorian/Riyadh formatter, preserved human due-date labels and stored values, and added focused display regressions. Focused unit 2 files / 19 tests and component 3 files / 26 tests pass; typecheck passes. Initial lint was interrupted without diagnostics; a fresh focused retry passed (exit 0). Preview/owner acceptance remains pending; see `evidence/delegation-queue.md`.
  - [x] X010-B-7C-11 Local rollover recovery correction: actual CLI failure-injection coverage (20 tests) covers rollback source-restore failure, compensation errors, and binding-present reapply failure; recovery verifies the active set and reports incomplete recovery explicitly. Fixed the password-map startup defect exposed by actual CLI execution. Exact run/source scope and history are preserved. Lead combined unit 41/41, full unit 74/400, focused lint, typecheck and syntax checks pass. No crash/concurrency/atomicity guarantee or hosted approval; real-DB/9E rehearsal remains pending.
  - [x] X010-B-7C-12 Local drawer execution-task feedback correction: Arabic field errors with accessible IDs/focus, nullable optional controls, preserved input/retry feedback, and stable keys for unchanged retries within the mounted form. Lead RED 11 failures followed by GREEN 21/21 including capability tests; corrected React-ref lint issue without suppression. Full component 33/178, full unit 74/400, focused lint and typecheck pass. Browser/visual/Preview/owner QA remains pending.
  - [x] X010-B-7C-13 Approved compact My Work list, locally verified (2026-09-07): replaced large list preview cards with operational rows; preserved scoped image thumbnails/fallbacks, owner/date/state/SLA/next action/counts, author-written stage labels, filtering, existing drawer and unchanged Kanban. D10 implementation and D11 browser checks were independently reviewed by lead and read-only D12. Worker RED/GREEN and lead mixed-language stage RED/GREEN recorded. Focused component 11/11, full component 36 files/232 tests, full fixture visual 15/15 (3.9m), typecheck, scoped lint/diff and production build PASS. Desktop/mobile screenshots inspected and saved. No fixed-height clipping, new dependency, schema/permission change or ADR. Other surface-density, whole-row activation, image-load error fallback, real-DB/hosted/CI and all 20 owner gates remain open; see delegation queue and visual evidence.
  - [x] X010-B-7C-14 / D13: local UX correction verified 2026-09-07 — mobile full-width search and side-by-side priority/SLA controls; native drawer button has whole-row primary hit area. Preserved 44px targets, scoped metadata, keyboard focus return and Kanban. Delegated RED/GREEN and read-only cross-review; full fixture browser 15/15, lead screenshot inspection, component 37/248, typecheck, scoped lint/diff and build PASS. Initial typecheck test-option error corrected without product changes. Owner/hosted acceptance remains pending.
  - [x] X010-B-7C-15 / D14: S015-P2-140 locally corrected — honest image/video load failure fallback, rejection handling and safe reset across file changes; late requests ignored, including A→B→A. Signed-preview boundary and fit behavior retained; no raw URL fallback/retry loops. Delegated RED/GREEN, independent cross-review, focused media 13/13 and combined gates above PASS. Real binary decoding, signed-URL expiry and hosted owner verification remain unexecuted.
  - [x] X010-B-7C-16 / D15: locally verified 2026-09-07 — compact mobile shell utilities/breadcrumbs, preserved desktop/navigation and 44px controls. Browser RED before layout; four persistent client keyboard-reveal failures reproduced and corrected via nearest link focus scrolling. Final shell/client-inbox browser 19 PASS / 20 intentional profile skips (7.8m); component 37/254, unit 2/9, scoped lint/diff, typecheck and fresh build PASS. No new navigation system, permission change or ADR; management fixture navigation remains component-only coverage. External/owner gates remain open.
  - [x] X010-B-7C-17 / D16: locally verified 2026-09-07 — real InternalTeamDirectory compact responsive rows preserve all fields/status/count/order and empty/admin states, without invented actions or text clipping. Guarded real-component fixture; initial geometry RED then directory 12/12 browser PASS including denied actors across three profiles. Shared final gates above PASS; lead inspected desktop/mobile images. Persistent authorization/queries unchanged; no real-DB/hosted/owner acceptance implied.
  - Requirements-quality gate: `checklists/owner-acceptance-requirements.md`.
  - X010-B-7C-10 continuation D08/D09 (2026-09-07): approval-panel due labels and client/management list date display locally corrected in disjoint parallel slices, preserving human labels/date precedence/ISO form values/permissions. Worker RED/GREEN and independent lead source/test review complete; combined D07-D09 component 7 files/75 tests, scoped lint and full typecheck PASS. One old display assertion updated after observed failure. Post-change browser/owner gates remain open; no real-DB/hosted action.
  - X010-B-7C-10 continuation D07 (2026-09-07): shared deliverable-card date and channel/format footer locally corrected using existing Arabic helpers. Lead-reviewed two-file scope; worker date/footer RED then GREEN, lead component 14/14, scoped lint, typecheck and diff check PASS. No layout or permission change. Post-change browser/owner acceptance and the confirmed client-approval-panel raw dueDateLabel residual remain open; see `evidence/delegation-queue.md`.

## Correction note: task assignment authority (2026-07-15)

Checkpoint 1A was reopened for a third corrective slice. Five defects (S015-P1-044 through S015-P1-047, S015-P2-048) were registered and addressed by additive migration `202607140005_s015_task_assignment_authority_correction.sql`: (1) `created_by` is no longer a permanent task-read grant; (2) deliverables SELECT RLS is narrowed so team roles see only owner/contributor/task-assignee deliverables while management sees all; (3) `s015_upsert_deliverable_task` restructures update authority into management/owner-contributor/assignee tiers with server-side protected-field preservation; (4) `s015_validate_task_assignee` links active role to active membership; (5) explicit server capabilities replace implicit UI inference. The canonical assignment model is documented in `defect-register.md`. This slice was superseded by the fourth bounded review correction below and remains part of the accepted Checkpoint 1A evidence.

Independent review reopened Checkpoint 1A for a fourth bounded corrective slice. Defects S015-P1-049 through S015-P1-052, S015-P1-054, and S015-P2-053/S015-P2-055/S015-P2-056 cover false service-role persona evidence, mutually recursive deliverable/task RLS, actor-unscoped capability calculation plus an invalid eligible-assignee query, task-only `/work` filtering, inactive-former-assignee RPC access, unwired/fail-open or actor-mismatched task controls, and late-table persistent harness grants. Additive migration `202607150001_s015_task_assignment_review_corrections.sql`, real-Auth persistent browser coverage, management task edit/reassign UI, exact-assignee status controls, and fail-closed capabilities passed the complete exact-HEAD PR #37 quality matrix in run `29404575276` for head SHA `98a6e6745cf5e6c13e76e672a44883ec0bd51201`. Checkpoint 1A is locally accepted with no open P0/P1. Those hosted items were historically closed by `evidence/hosted-team-uat-final-handoff.md`, then reopened by the 2026-07-19 owner rejection; their current state is governed by X008-H above. Production acceptance remains open and outside this task.

## Correction note: persistent browser verification

On 2026-07-11, T013, T016, and T017 were reopened after defect `S015-P1-019` identified that the prior Playwright path used route actor fixtures under `APP_ENV=test` and therefore did not prove a real browser-to-persistent-database journey. They were closed again only after `npm run test:e2e:persistent` passed against the local Supabase API/Auth stack with `APP_ENV=test-persistent`, route fixtures disabled, synthetic Auth users, and DB assertions for version binding, role boundaries, comments/files secrecy, SLA, audit, package ledger, and delivery.
